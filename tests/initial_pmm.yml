---
- hosts: all:!ansible
  gather_facts: True
  remote_user: root
  vars:
    install_percona_tools: False # Whether to install percona tools
    auto_config_pmm: False # Whether to config pmm client connect to pmm server
    config_mysql: False
    config_proxysql: False
    config_mongodb: False
  roles:
    - dba_ray.ansible
