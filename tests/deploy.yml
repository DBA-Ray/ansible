---
- hosts: localhost
  gather_facts: True
  remote_user: root
  vars:
    is_ansible_tower: False
    is_download_roles_packages: False
    mysql_config_editor: False
  roles:
    - dba_ray.ansible
